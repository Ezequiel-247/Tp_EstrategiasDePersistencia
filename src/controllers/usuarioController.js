const { usuario } = require('../db/models')

//aca los metodos get post put y delete

module.exports = {
    /*obtenerUsuarios,
    obtenerUsuario,
    crearUsuario,
    actualizarUsuario,
    eliminarUsuario*/ 
}